import {    LOADED_SHOPS,    FETCH_SHOPS,} from '../Actions/shops'export const SHOPS_DEFAULT_STATE = {    loading: false,    saving: false,    error: '',    items: []}export default function shops (state = SHOPS_DEFAULT_STATE, action) {    switch (action.type) {        case LOADED_SHOPS:            return {                ...state,                items: action.shops,                loading: false            }                case FETCH_SHOPS: {            return {                ...state,                loading: true            }        }        default:            return state    }}