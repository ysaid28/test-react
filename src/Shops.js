import React, {Component} from 'react'import {connect} from 'react-redux'import {    fetchShops,} from './Actions/shops';class Shop extends Component {    render() {        const {shop} = this.props;        return (            <div className="col-md-12">                <div className="media">                    <div className="media-left">                        <img src={shop.logo} className="media-object img-fluid" width="200" height="200"/>                    </div>                    <div className="media-body">                        <h4 className="media-heading">{shop.name}</h4>                        <ul className="list-unstyled">                            <li>shop.category_name}</li>                            <li>{shop.city}</li>                            <li>{shop.id}</li>                        </ul>                    </div>                </div>                <hr/>            </div>        )    }}class Shops extends Component {    componentDidMount() {        this.props.fetchShops()    }    render() {        const {shops} = this.props        console.log(shops);        console.log(shops.results);        return (            <div className='row'>                {shops.results ? shops.results.map((shop, index) =>                    <Shop                        id={shop.id}                        key={index}                        shop={shop}                    />                ) : 'Data not found'}            </div>        )    }}const mapStateToProps = (state) => {    return {        shops: state.shops.items,        isLoading: state.shops.loading,        error: state.shops.error    }}const mapDispatchToProps = {    fetchShops,}export default connect(mapStateToProps, mapDispatchToProps)(Shops)